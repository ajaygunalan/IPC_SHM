#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("Hello, World! \n");
		int i;
	for (i=0; i<8; i++)
	{
		printf("%d \n", i);
		//encoder_value_in_str[i] = "H";	
	}
   return 0;
}
